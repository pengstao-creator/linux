# make工具在执行的时候，会去找到Makefile配置文件来做构建
# make的默认配置文件名称就是Makefile
# 配置文件的名称也可以改，但是如果名称不是Makefile，那么make在执行的时候，需要手动指定配置文件

# Makefile中的格式:
# 目标:依赖(依赖可有可无)
# 	需要执行的命令
# 目标：就是在通过make执行 的时候，可以通过指定目标去指定要执行哪一个指令

task1:task2
	@echo "task1......................"
task2:task3
	@echo "task2......................"
task3:
	@echo "task3......................"

# Makefile文件写好后，下一步就可以通过make命令来执行
# make命令的完整写法: make 要执行的目标(目标也可以不写，如果不写目标，那么默认就是要执行第一个目标) -f 配置文件名称(如果名称是Makefile，可以g省略不写)
# make task1 -f Makefile   =>  make